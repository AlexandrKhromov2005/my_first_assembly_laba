# Чек-лист проверки лабораторной работы №3

## ✅ Основное задание (обязательно)

### Пункт 3: Создание make-файла
- [x] Создан файл `Makefile` в корне проекта
- [x] Make-файл компилирует программу на C
- [x] Make-файл компилирует код на ассемблере
- [x] Используются правила и зависимости

### Пункт 4: Компиляция и выполнение
```bash
# Проверка
make
make run
```
- [x] Программа успешно компилируется
- [x] Программа запускается без ошибок
- [x] Вывод корректный

### Пункт 5: Инкрементальная сборка
```bash
# Проверка
make clean && make
stat -c "%y %n" bin/obj/*.o bin/matrix_rotate
sleep 1 && touch src/main.c
make
stat -c "%y %n" bin/obj/*.o bin/matrix_rotate
```
- [x] При первой сборке компилируются все файлы
- [x] При изменении одного файла перекомпилируется только он
- [x] Неизменённые файлы НЕ перекомпилируются
- [x] Время модификации изменяется только у изменённых файлов

---

## ✅ Дополнительное задание (пункт 6)

### Высокий уровень автоматизации

#### Переменные
```bash
# Проверка
grep "PROGRAM_NAME\|SRC_DIR\|BIN_DIR" Makefile
```
- [x] Имя программы задаётся переменной
- [x] Флаги компиляции задаются переменными
- [x] Директории задаются переменными

#### Динамические зависимости
```bash
# Проверка
ls bin/obj/*.d
cat bin/obj/main.d
```
- [x] Зависимости формируются автоматически
- [x] Файлы `.d` создаются при компиляции
- [x] Зависимости включаются в Makefile

#### Автоматическое определение файлов
```bash
# Проверка
grep "wildcard\|patsubst" Makefile
```
- [x] Используется `wildcard` для поиска исходников
- [x] Используется `patsubst` для формирования объектных файлов

#### Цели очистки
```bash
# Проверка
make clean
make distclean
```
- [x] Есть цель `clean` (удаление объектных файлов)
- [x] Есть цель `distclean` (полная очистка)

#### Структура проекта
```bash
# Проверка
ls -la src/ bin/ bin/obj/
```
- [x] Директория `src/` с исходниками
- [x] Директория `bin/` с бинарниками
- [x] Директория `bin/obj/` с объектными файлами

---

## ✅ Дополнительное задание (пункт 7)

### Динамическое изменение функциональности

#### Управление методом компиляции
```bash
# Проверка
make SYNTAX=intel
make clean && make SYNTAX=att
```
- [x] Выбор синтаксиса ассемблера (Intel/AT&T)
- [x] Разные ассемблеры используются корректно

#### Управление отладкой
```bash
# Проверка
make DEBUG=yes
file bin/matrix_rotate | grep debug
```
- [x] Режим отладки включается через DEBUG=yes
- [x] Отладочные символы добавляются в исполняемый файл

#### Управление дополнительными флагами
```bash
# Проверка
make COMPILE_FLAGS="-O2"
make info | grep "Flags"
```
- [x] Можно передавать дополнительные флаги компиляции
- [x] Флаги применяются корректно

#### Комбинированное использование
```bash
# Проверка
make SYNTAX=att DEBUG=yes COMPILE_FLAGS="-O2"
```
- [x] Можно комбинировать несколько параметров
- [x] Все параметры работают одновременно

---

## ✅ Дополнительные проверки

### Используемые возможности make

#### Переменные
```bash
grep "^[A-Z_]*\s*[:?]=" Makefile | head -10
```
- [x] Используются обычные переменные (`:=`)
- [x] Используются условные переменные (`?=`)
- [x] Используется `override` для переопределения

#### Функции
```bash
grep "wildcard\|patsubst\|addprefix\|addsuffix\|notdir\|basename" Makefile
```
- [x] `wildcard` - поиск файлов
- [x] `patsubst` - замена паттернов
- [x] `basename` - извлечение имени
- [x] `addprefix` / `addsuffix` - добавление префикса/суффикса

#### Автоматические переменные
```bash
grep '\$@\|\$<\|\$\^' Makefile
```
- [x] `$@` - имя цели
- [x] `$<` - первая зависимость
- [x] `$^` - все зависимости

#### Шаблонные правила
```bash
grep "%.o:" Makefile
```
- [x] Шаблонное правило для `.c → .o`
- [x] Шаблонное правило для `.asm → .o`
- [x] Шаблонное правило для `.s → .o`

#### Условная компиляция
```bash
grep "ifeq\|else\|endif" Makefile
```
- [x] Используется `ifeq/else/endif`
- [x] Условия работают корректно

#### Фиктивные цели
```bash
grep "^\.PHONY:" Makefile
```
- [x] Объявлены все служебные цели

#### Order-only prerequisites
```bash
grep "| \$(.*_DIR)" Makefile
```
- [x] Используется `|` для директорий

---

## 📋 Финальная проверка

### Команды для демонстрации

```bash
# 1. Справка
make help

# 2. Конфигурация
make info

# 3. Обычная сборка
make distclean && make

# 4. Запуск
make run

# 5. Тесты
make test

# 6. Инкрементальная сборка
make clean && make
stat -c "%y %n" bin/obj/*.o
touch src/main.c
make
stat -c "%y %n" bin/obj/*.o

# 7. AT&T синтаксис
make clean && make SYNTAX=att

# 8. Отладка
make clean && make DEBUG=yes
file bin/matrix_rotate

# 9. Комбинация
make clean && make SYNTAX=att DEBUG=yes COMPILE_FLAGS="-O2"

# 10. Очистка
make clean
make distclean
```

---

## ✅ ИТОГО

Если все пункты отмечены, то **лабораторная работа выполнена полностью!**

### Документация
- [x] LAB3_README.md - подробная документация
- [x] ОТЧЕТ_ЛР3.md - отчёт для преподавателя
- [x] QUICKSTART.md - краткая инструкция
- [x] CHECKLIST.md - этот чек-лист
- [x] DEMO.sh - демонстрационный скрипт

### Git
- [x] Все файлы добавлены в репозиторий
- [x] Создан информативный коммит
- [x] .gitignore настроен правильно
