# Теория: Утилита GNU Make

## Оглавление
1. [Введение в Make](#введение-в-make)
2. [Основные концепции](#основные-концепции)
3. [Синтаксис Makefile](#синтаксис-makefile)
4. [Переменные](#переменные)
5. [Автоматические переменные](#автоматические-переменные)
6. [Функции](#функции)
7. [Шаблонные правила](#шаблонные-правила)
8. [Условная компиляция](#условная-компиляция)
9. [Автоматическая генерация зависимостей](#автоматическая-генерация-зависимостей)
10. [Практические примеры](#практические-примеры)

---

## Введение в Make

### Что такое Make?

**Make** — это утилита для автоматизации процесса сборки программ. Она:
- Определяет, какие части программы нужно перекомпилировать
- Выполняет команды для обновления файлов
- Отслеживает зависимости между файлами
- Минимизирует время сборки (инкрементальная компиляция)

### Зачем нужен Make?

**Проблема:** При разработке большого проекта с множеством исходных файлов:
- Сложно запоминать команды компиляции
- Долго перекомпилировать весь проект
- Легко забыть пересобрать зависимые файлы

**Решение:** Make автоматически:
- Определяет, какие файлы изменились
- Перекомпилирует только необходимые файлы
- Соблюдает порядок сборки согласно зависимостям

### Версии Make

- **GNU Make** — самая популярная версия (используем в лабе)
- **BSD Make** — используется в BSD системах
- **Microsoft NMAKE** — для Windows

---

## Основные концепции

### 1. Правила (Rules)

Правило описывает, как создать или обновить файл.

**Синтаксис:**
```makefile
цель: зависимость1 зависимость2 ...
	команда1
	команда2
	...
```

**Важно:** Команды должны начинаться с **символа табуляции** (не пробелов)!

**Пример:**
```makefile
program: main.o utils.o
	gcc main.o utils.o -o program
```

### 2. Цели (Targets)

**Цель** — это результат, который нужно получить.

**Типы целей:**

#### Файловые цели
Цель является именем файла:
```makefile
main.o: main.c
	gcc -c main.c
```

#### Фиктивные цели (Phony Targets)
Цель является действием, а не файлом:
```makefile
.PHONY: clean
clean:
	rm -f *.o program
```

**Зачем .PHONY?**
- Если в директории есть файл с именем `clean`, Make не выполнит цель
- `.PHONY` говорит Make, что это действие, а не файл

### 3. Зависимости (Dependencies)

**Зависимость** — файл или цель, которые должны существовать/быть выполнены перед целью.

```makefile
program: main.o utils.o    # program зависит от main.o и utils.o
main.o: main.c utils.h      # main.o зависит от main.c и utils.h
```

**Типы зависимостей:**

#### Обычные зависимости
```makefile
main.o: main.c utils.h
	gcc -c main.c
```
Если `main.c` или `utils.h` изменились → `main.o` пересобирается

#### Order-only зависимости
```makefile
main.o: main.c | bin
	gcc -c main.c -o bin/main.o

bin:
	mkdir -p bin
```
Директория `bin` должна существовать, но её время модификации не влияет на пересборку

### 4. Команды

**Команды** — shell-команды для достижения цели.

```makefile
program: main.o
	gcc main.o -o program
	chmod +x program
	@echo "Build complete!"
```

**Префиксы команд:**
- `@` — не выводить команду на экран
- `-` — игнорировать ошибки
- `+` — выполнить, даже если указан `-n`

**Примеры:**
```makefile
test:
	@echo "Running tests..."    # Не показывать "echo"
	-./test_program              # Продолжить, даже если тест упал
```

### 5. Алгоритм работы Make

1. **Выбор главной цели:**
   - Указана в командной строке: `make clean`
   - Первая цель в Makefile

2. **Для каждой цели:**
   - Проверить зависимости (рекурсивно)
   - Определить, нужно ли выполнять команды

3. **Условия выполнения команд:**
   - Цель не существует
   - Цель — фиктивная (`.PHONY`)
   - Любая зависимость новее цели
   - Любая зависимость была пересобрана

**Пример:**
```makefile
program: main.o utils.o
	gcc main.o utils.o -o program

main.o: main.c
	gcc -c main.c

utils.o: utils.c
	gcc -c utils.c
```

**Ход выполнения `make`:**
1. Цель: `program`
2. Проверка зависимостей: `main.o`, `utils.o`
3. Для `main.o`: проверка `main.c` → компиляция
4. Для `utils.o`: проверка `utils.c` → компиляция
5. Линковка: `program`

---

## Переменные

### Типы переменных

#### 1. Простое присваивание (`:=`)
Значение вычисляется сразу:
```makefile
CC := gcc
CFLAGS := -Wall -O2
```

#### 2. Рекурсивное присваивание (`=`)
Значение вычисляется при использовании:
```makefile
OBJS = main.o utils.o
TARGET = program_$(VERSION)
VERSION = 1.0
```

#### 3. Условное присваивание (`?=`)
Присвоить, только если переменная не определена:
```makefile
CC ?= gcc           # Если CC не задан, использовать gcc
DEBUG ?= no         # Значение по умолчанию
```

#### 4. Добавление (`+=`)
Добавить к существующему значению:
```makefile
CFLAGS := -Wall
CFLAGS += -O2       # Теперь: -Wall -O2
```

#### 5. Override
Переопределить переменную из командной строки:
```makefile
override CFLAGS += -pipe
```
Даже если `make CFLAGS="-O2"`, добавится `-pipe`

### Использование переменных

```makefile
# Определение
CC := gcc
CFLAGS := -Wall -O2
SOURCES := main.c utils.c

# Использование
$(CC) $(CFLAGS) $(SOURCES) -o program

# Или
${CC} ${CFLAGS} ${SOURCES} -o program
```

### Встроенные переменные

```makefile
CC       # Компилятор C (по умолчанию: cc)
CXX      # Компилятор C++ (по умолчанию: g++)
CFLAGS   # Флаги для компилятора C
LDFLAGS  # Флаги для линковщика
AR       # Архиватор (по умолчанию: ar)
RM       # Команда удаления (по умолчанию: rm -f)
```

### Переменные окружения

Make автоматически импортирует переменные окружения:
```bash
export CUSTOM_VAR=value
make
```

```makefile
# В Makefile доступна CUSTOM_VAR
$(info Custom var: $(CUSTOM_VAR))
```

---

## Автоматические переменные

Автоматические переменные генерируются Make для каждого правила.

### Основные автоматические переменные

| Переменная | Описание | Пример |
|------------|----------|--------|
| `$@` | Имя цели | `program` |
| `$<` | Первая зависимость | `main.c` |
| `$^` | Все зависимости | `main.c utils.c` |
| `$?` | Зависимости новее цели | `main.c` |
| `$*` | Базовое имя (без расширения) | `main` |
| `$(@D)` | Директория цели | `bin` |
| `$(@F)` | Имя файла цели | `program` |
| `$(<D)` | Директория первой зависимости | `src` |
| `$(<F)` | Имя первой зависимости | `main.c` |

### Примеры использования

```makefile
# $@ - имя цели
program: main.o utils.o
	gcc main.o utils.o -o $@
	# $@ заменится на "program"

# $< - первая зависимость
main.o: main.c
	gcc -c $< -o $@
	# $< = main.c, $@ = main.o

# $^ - все зависимости
program: main.o utils.o config.o
	gcc $^ -o $@
	# $^ = main.o utils.o config.o

# $? - только новые зависимости
library.a: $(OBJECTS)
	ar rcs $@ $?
	# $? = только изменённые объектные файлы

# $* - базовое имя (в шаблонных правилах)
%.o: %.c
	gcc -c $< -o $@
	# Для main.c: $* = main
```

### Практический пример

```makefile
CC := gcc
CFLAGS := -Wall -O2

# Без автоматических переменных
main.o: main.c
	gcc -Wall -O2 -c main.c -o main.o

# С автоматическими переменными
main.o: main.c
	$(CC) $(CFLAGS) -c $< -o $@
	# Короче и гибче!
```

---

## Функции

Make предоставляет функции для обработки текста.

### Синтаксис

```makefile
$(function arguments)
# или
${function arguments}
```

### Функции для работы со строками

#### `$(subst from,to,text)`
Заменить подстроку:
```makefile
SOURCES := main.c utils.c test.c
OBJECTS := $(subst .c,.o,$(SOURCES))
# OBJECTS = main.o utils.o test.o
```

#### `$(patsubst pattern,replacement,text)`
Заменить по паттерну:
```makefile
SOURCES := src/main.c src/utils.c
OBJECTS := $(patsubst src/%.c,obj/%.o,$(SOURCES))
# OBJECTS = obj/main.o obj/utils.o
```

#### `$(strip string)`
Удалить лишние пробелы:
```makefile
TEXT := "  hello   world  "
CLEAN := $(strip $(TEXT))
# CLEAN = "hello world"
```

#### `$(filter pattern,text)`
Выбрать подходящие слова:
```makefile
FILES := main.c utils.o test.c data.txt
C_FILES := $(filter %.c,$(FILES))
# C_FILES = main.c test.c
```

#### `$(filter-out pattern,text)`
Исключить подходящие слова:
```makefile
FILES := main.c utils.o test.c
NO_TEST := $(filter-out test%,$(FILES))
# NO_TEST = main.c utils.o
```

### Функции для работы с файлами

#### `$(wildcard pattern)`
Найти файлы по шаблону:
```makefile
SOURCES := $(wildcard src/*.c)
# Все .c файлы в src/
```

#### `$(dir names)`
Извлечь директорию:
```makefile
PATH := src/module/main.c
DIR := $(dir $(PATH))
# DIR = src/module/
```

#### `$(notdir names)`
Извлечь имя файла:
```makefile
PATH := src/module/main.c
FILE := $(notdir $(PATH))
# FILE = main.c
```

#### `$(basename names)`
Убрать расширение:
```makefile
FILE := main.c
BASE := $(basename $(FILE))
# BASE = main
```

#### `$(suffix names)`
Извлечь расширение:
```makefile
FILE := main.c
EXT := $(suffix $(FILE))
# EXT = .c
```

#### `$(addsuffix suffix,names)`
Добавить суффикс:
```makefile
NAMES := main utils test
C_FILES := $(addsuffix .c,$(NAMES))
# C_FILES = main.c utils.c test.c
```

#### `$(addprefix prefix,names)`
Добавить префикс:
```makefile
FILES := main.c utils.c
SRC_FILES := $(addprefix src/,$(FILES))
# SRC_FILES = src/main.c src/utils.c
```

### Функции для списков

#### `$(word n,text)`
n-е слово (начиная с 1):
```makefile
LIST := one two three
SECOND := $(word 2,$(LIST))
# SECOND = two
```

#### `$(words text)`
Количество слов:
```makefile
LIST := one two three
COUNT := $(words $(LIST))
# COUNT = 3
```

#### `$(firstword text)`
Первое слово:
```makefile
LIST := one two three
FIRST := $(firstword $(LIST))
# FIRST = one
```

#### `$(lastword text)`
Последнее слово:
```makefile
LIST := one two three
LAST := $(lastword $(LIST))
# LAST = three
```

### Функции для вызова команд

#### `$(shell command)`
Выполнить shell-команду:
```makefile
DATE := $(shell date +%Y-%m-%d)
FILES_COUNT := $(shell ls *.c 2>/dev/null | wc -l)

all:
	@echo "Build date: $(DATE)"
	@echo "C files: $(FILES_COUNT)"
```

#### `$(eval text)`
Выполнить текст как код Makefile:
```makefile
define RULE_TEMPLATE
$(1).o: $(1).c
	gcc -c $$< -o $$@
endef

$(eval $(call RULE_TEMPLATE,main))
$(eval $(call RULE_TEMPLATE,utils))
```

### Условные функции

#### `$(if condition,then-part,else-part)`
```makefile
DEBUG := yes
CFLAGS := $(if $(filter yes,$(DEBUG)),-g,-O2)
# CFLAGS = -g (если DEBUG=yes)
```

#### `$(or condition1,condition2,...)`
```makefile
VALUE := $(or $(VAR1),$(VAR2),default)
# Первое непустое значение
```

#### `$(and condition1,condition2,...)`
```makefile
RESULT := $(and $(VAR1),$(VAR2))
# Пусто, если любое пусто
```

### Практический пример

```makefile
# Найти все .c файлы
SOURCES := $(wildcard src/*.c)

# Преобразовать в .o файлы
OBJECTS := $(patsubst src/%.c,obj/%.o,$(SOURCES))

# Добавить директорию include
INCLUDES := $(addprefix -I,$(wildcard include/*))

# Вывести информацию
$(info Sources: $(SOURCES))
$(info Objects: $(OBJECTS))
$(info Includes: $(INCLUDES))
```

---

## Шаблонные правила

Шаблонные правила описывают, как обрабатывать целую категорию файлов.

### Синтаксис

```makefile
%.o: %.c
	gcc -c $< -o $@
```

`%` — подстановочный символ (wildcard)

### Примеры

#### Компиляция C в объектные файлы
```makefile
%.o: %.c
	gcc -c -Wall $< -o $@

# Применяется к:
# main.c → main.o
# utils.c → utils.o
# test.c → test.o
```

#### Компиляция C++ в объектные файлы
```makefile
%.o: %.cpp
	g++ -c -std=c++17 $< -o $@
```

#### Ассемблер в объектные файлы
```makefile
# Intel синтаксис
%.o: %.asm
	nasm -f elf32 $< -o $@

# AT&T синтаксис
%.o: %.s
	as --32 $< -o $@
```

#### Преобразование в разные директории
```makefile
obj/%.o: src/%.c
	gcc -c $< -o $@

# src/main.c → obj/main.o
# src/utils.c → obj/utils.o
```

### Множественные зависимости

```makefile
# Все .o файлы зависят от header.h
%.o: %.c header.h
	gcc -c $< -o $@
```

### Переменная $*

В шаблонных правилах `$*` — базовое имя (без расширения):

```makefile
%.o: %.c
	@echo "Compiling $* from $< to $@"
	gcc -c $< -o $@

# Для main.c:
# $* = main
# $< = main.c
# $@ = main.o
```

### Статические шаблонные правила

```makefile
OBJECTS := main.o utils.o test.o

# Только для этих файлов
$(OBJECTS): %.o: %.c
	gcc -c $< -o $@
```

### Старый суффиксный синтаксис

**Устаревший (не рекомендуется):**
```makefile
.c.o:
	gcc -c $<
```

**Современный:**
```makefile
%.o: %.c
	gcc -c $< -o $@
```

---

## Условная компиляция

### Синтаксис

```makefile
ifeq (arg1,arg2)
    # команды, если равны
else
    # команды, если не равны
endif
```

### Директивы

- `ifeq (arg1,arg2)` — если равны
- `ifneq (arg1,arg2)` — если не равны
- `ifdef variable` — если переменная определена
- `ifndef variable` — если переменная не определена

### Примеры

#### Проверка на равенство
```makefile
DEBUG ?= no

ifeq ($(DEBUG),yes)
    CFLAGS += -g -DDEBUG
else
    CFLAGS += -O2
endif
```

#### Проверка на неравенство
```makefile
PLATFORM ?= linux

ifneq ($(PLATFORM),windows)
    CFLAGS += -fPIC
endif
```

#### Проверка определения переменной
```makefile
ifdef CUSTOM_CC
    CC := $(CUSTOM_CC)
else
    CC := gcc
endif
```

#### Проверка неопределения переменной
```makefile
ifndef PREFIX
    PREFIX := /usr/local
endif
```

#### Множественные условия
```makefile
ifeq ($(DEBUG),yes)
    CFLAGS += -g
    ifeq ($(VERBOSE),yes)
        CFLAGS += -v
    endif
else
    CFLAGS += -O2
endif
```

#### Выбор из нескольких вариантов
```makefile
ARCH ?= x86

ifeq ($(ARCH),x86)
    CFLAGS += -m32
else ifeq ($(ARCH),x64)
    CFLAGS += -m64
else ifeq ($(ARCH),arm)
    CC := arm-linux-gcc
else
    $(error Unknown architecture: $(ARCH))
endif
```

### Условия на основе функций

```makefile
# Проверка наличия файла
ifneq ($(wildcard config.mk),)
    include config.mk
endif

# Проверка на пустоту
ifeq ($(strip $(SOURCES)),)
    $(error No source files found)
endif

# Фильтрация
ifneq ($(filter yes,$(DEBUG)),)
    CFLAGS += -g
endif
```

### Практический пример

```makefile
# Определение платформы
UNAME := $(shell uname)

ifeq ($(UNAME),Linux)
    PLATFORM := linux
    LDFLAGS += -pthread
else ifeq ($(UNAME),Darwin)
    PLATFORM := macos
    LDFLAGS += -framework CoreFoundation
else ifeq ($(UNAME),Windows_NT)
    PLATFORM := windows
    EXE_EXT := .exe
else
    $(error Unsupported platform: $(UNAME))
endif

# Режим сборки
DEBUG ?= no

ifeq ($(DEBUG),yes)
    BUILD_TYPE := debug
    CFLAGS += -g -O0 -DDEBUG
    OUT_DIR := build/debug
else
    BUILD_TYPE := release
    CFLAGS += -O3 -DNDEBUG
    OUT_DIR := build/release
endif

$(info Building $(BUILD_TYPE) for $(PLATFORM))
```

---

## Автоматическая генерация зависимостей

### Проблема

```makefile
main.o: main.c utils.h config.h types.h
	gcc -c main.c
```

**Проблемы:**
- Нужно вручную перечислять все заголовки
- Легко забыть файл
- При изменении `#include` нужно обновлять Makefile

### Решение: флаг -MD

GCC может автоматически генерировать файлы зависимостей:

```bash
gcc -MD -c main.c -o main.o
```

Создаётся файл `main.d`:
```makefile
main.o: main.c utils.h config.h types.h \
  /usr/include/stdio.h /usr/include/stdlib.h
```

### Флаги GCC

- `-M` — только зависимости, не компилировать
- `-MM` — только пользовательские заголовки
- `-MD` — зависимости + компиляция
- `-MMD` — только пользовательские + компиляция

### Интеграция в Makefile

#### Базовый вариант

```makefile
CC := gcc
CFLAGS := -Wall -O2

# Компиляция с генерацией зависимостей
%.o: %.c
	$(CC) $(CFLAGS) -MD -c $< -o $@

# Включение всех файлов .d
-include $(wildcard *.d)
```

**Важно:** `-include` (с минусом) не выдаёт ошибку, если файлы не существуют.

#### Продвинутый вариант

```makefile
SRC_DIR := src
OBJ_DIR := obj

SOURCES := $(wildcard $(SRC_DIR)/*.c)
OBJECTS := $(patsubst $(SRC_DIR)/%.c,$(OBJ_DIR)/%.o,$(SOURCES))
DEPENDS := $(OBJECTS:.o=.d)

# Компиляция с зависимостями
$(OBJ_DIR)/%.o: $(SRC_DIR)/%.c | $(OBJ_DIR)
	$(CC) $(CFLAGS) -MD -MP -c $< -o $@

# Создание директории
$(OBJ_DIR):
	mkdir -p $@

# Включение зависимостей
-include $(DEPENDS)

# Очистка
clean:
	rm -rf $(OBJ_DIR)
```

### Флаг -MP

Флаг `-MP` создаёт фиктивные цели для заголовочных файлов:

**Без -MP:**
```makefile
main.o: main.c utils.h
```

**С -MP:**
```makefile
main.o: main.c utils.h

utils.h:
```

**Зачем?** Если `utils.h` будет удалён, Make не выдаст ошибку "No rule to make target".

### Полный пример

```makefile
# Настройки
CC := gcc
CFLAGS := -Wall -O2 -Iinclude
SRC_DIR := src
OBJ_DIR := obj
BIN_DIR := bin

# Файлы
SOURCES := $(wildcard $(SRC_DIR)/*.c)
OBJECTS := $(patsubst $(SRC_DIR)/%.c,$(OBJ_DIR)/%.o,$(SOURCES))
DEPENDS := $(OBJECTS:.o=.d)
TARGET := $(BIN_DIR)/program

# Главная цель
all: $(TARGET)

# Линковка
$(TARGET): $(OBJECTS) | $(BIN_DIR)
	$(CC) $^ -o $@

# Компиляция с автозависимостями
$(OBJ_DIR)/%.o: $(SRC_DIR)/%.c | $(OBJ_DIR)
	$(CC) $(CFLAGS) -MD -MP -c $< -o $@

# Создание директорий
$(BIN_DIR) $(OBJ_DIR):
	mkdir -p $@

# Включение зависимостей
-include $(DEPENDS)

# Очистка
clean:
	rm -f $(OBJ_DIR)/*.o $(OBJ_DIR)/*.d

distclean: clean
	rm -f $(TARGET)
	rm -rf $(BIN_DIR) $(OBJ_DIR)

.PHONY: all clean distclean
```

### Как это работает

1. **Первая сборка:**
   - Компилируются все `.c` файлы
   - Создаются `.d` файлы с зависимостями
   - Make включает `.d` файлы (но они пока пустые)

2. **Последующие сборки:**
   - Make читает `.d` файлы
   - Знает все зависимости
   - Пересобирает только изменённые файлы

3. **Изменение заголовка:**
   - `.d` файл указывает на зависимость
   - Make автоматически пересобирает нужные файлы

---

## Практические примеры

### Пример 1: Простой проект на C

```makefile
# Компилятор и флаги
CC := gcc
CFLAGS := -Wall -O2

# Файлы
SOURCES := main.c utils.c config.c
OBJECTS := $(SOURCES:.c=.o)
TARGET := program

# Главная цель
all: $(TARGET)

# Линковка
$(TARGET): $(OBJECTS)
	$(CC) $(OBJECTS) -o $(TARGET)

# Компиляция
%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# Очистка
clean:
	rm -f $(OBJECTS) $(TARGET)

.PHONY: all clean
```

### Пример 2: Проект с разными языками

```makefile
# Компиляторы
CC := gcc
CXX := g++
AS := nasm

# Файлы
C_SOURCES := $(wildcard *.c)
CPP_SOURCES := $(wildcard *.cpp)
ASM_SOURCES := $(wildcard *.asm)

OBJECTS := $(C_SOURCES:.c=.o) \
           $(CPP_SOURCES:.cpp=.o) \
           $(ASM_SOURCES:.asm=.o)

TARGET := mixed_program

# Сборка
all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CXX) $^ -o $@

# Правила компиляции
%.o: %.c
	$(CC) -c $< -o $@

%.o: %.cpp
	$(CXX) -c $< -o $@

%.o: %.asm
	$(AS) -f elf64 $< -o $@

clean:
	rm -f $(OBJECTS) $(TARGET)

.PHONY: all clean
```

### Пример 3: Библиотека

```makefile
# Настройки
CC := gcc
AR := ar
CFLAGS := -Wall -O2 -fPIC

# Файлы
SOURCES := $(wildcard src/*.c)
OBJECTS := $(SOURCES:.c=.o)
STATIC_LIB := libmylib.a
SHARED_LIB := libmylib.so

# Цели
all: static shared

static: $(STATIC_LIB)
shared: $(SHARED_LIB)

# Статическая библиотека
$(STATIC_LIB): $(OBJECTS)
	$(AR) rcs $@ $^

# Динамическая библиотека
$(SHARED_LIB): $(OBJECTS)
	$(CC) -shared $^ -o $@

# Компиляция
%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

# Установка
install: all
	install -d $(PREFIX)/lib
	install -m 644 $(STATIC_LIB) $(PREFIX)/lib/
	install -m 755 $(SHARED_LIB) $(PREFIX)/lib/

clean:
	rm -f $(OBJECTS) $(STATIC_LIB) $(SHARED_LIB)

.PHONY: all static shared install clean
```

### Пример 4: Кроссплатформенная сборка

```makefile
# Определение платформы
UNAME := $(shell uname -s)

ifeq ($(UNAME),Linux)
    CC := gcc
    CFLAGS := -pthread
    LDFLAGS := -ldl
    EXE :=
else ifeq ($(UNAME),Darwin)
    CC := clang
    CFLAGS := -pthread
    LDFLAGS := -framework CoreFoundation
    EXE :=
else ifeq ($(OS),Windows_NT)
    CC := gcc
    CFLAGS := -D_WIN32
    LDFLAGS := -lws2_32
    EXE := .exe
endif

TARGET := program$(EXE)
SOURCES := $(wildcard *.c)
OBJECTS := $(SOURCES:.c=.o)

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $^ $(LDFLAGS) -o $@

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJECTS) $(TARGET)

.PHONY: all clean
```

### Пример 5: Многодиректорийный проект

```makefile
# Структура:
# project/
#   ├── src/
#   │   ├── main.c
#   │   └── module/
#   │       └── utils.c
#   ├── include/
#   │   └── utils.h
#   ├── build/
#   └── Makefile

CC := gcc
CFLAGS := -Wall -O2 -Iinclude

SRC_DIRS := src src/module
BUILD_DIR := build

# Найти все .c файлы
SOURCES := $(foreach dir,$(SRC_DIRS),$(wildcard $(dir)/*.c))

# obj/src/main.o, obj/src/module/utils.o
OBJECTS := $(patsubst %.c,$(BUILD_DIR)/%.o,$(SOURCES))

# Создать список директорий
OBJ_DIRS := $(sort $(dir $(OBJECTS)))

TARGET := $(BUILD_DIR)/program

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) $^ -o $@

# Создать директории перед компиляцией
$(OBJECTS): | $(OBJ_DIRS)

$(OBJ_DIRS):
	mkdir -p $@

$(BUILD_DIR)/%.o: %.c
	$(CC) $(CFLAGS) -MD -c $< -o $@

-include $(OBJECTS:.o=.d)

clean:
	rm -rf $(BUILD_DIR)

.PHONY: all clean
```

---

## Дополнительные возможности

### 1. Include других файлов

```makefile
# Включить другой makefile
include config.mk

# Условное включение
-include optional.mk  # Не ошибка, если не существует
```

### 2. Экспорт переменных

```makefile
# Экспорт в подпроцессы
export CC := gcc
export CFLAGS := -Wall

# Экспорт всех переменных
.EXPORT_ALL_VARIABLES:
```

### 3. Многострочные переменные

```makefile
define HELP_TEXT
Usage: make [target]

Targets:
  all     - Build everything
  clean   - Remove built files
  test    - Run tests
endef

export HELP_TEXT

help:
	@echo "$$HELP_TEXT"
```

### 4. Рекурсивный Make

```makefile
SUBDIRS := module1 module2 module3

all:
	for dir in $(SUBDIRS); do \
		$(MAKE) -C $$dir; \
	done

# Или лучше:
all: $(SUBDIRS)

$(SUBDIRS):
	$(MAKE) -C $@

.PHONY: all $(SUBDIRS)
```

### 5. Параллельная сборка

```bash
# Запуск с 4 параллельными задачами
make -j4

# Максимальный параллелизм
make -j
```

В Makefile:
```makefile
# Ограничить параллелизм для конкретной цели
.NOTPARALLEL: install
```

### 6. Вывод информации

```makefile
$(info This is informational message)
$(warning This is a warning)
$(error This is an error - stops make)

# Пример
ifeq ($(CC),)
    $(error Compiler not defined!)
endif
```

---

## Полезные советы

### 1. Отладка Makefile

```bash
# Вывести все команды
make -n

# Вывести переменные
make -p

# Отладочный вывод
make --debug

# Трассировка
make --trace
```

В Makefile:
```makefile
# Вывести значение переменной
$(info CC = $(CC))
$(info SOURCES = $(SOURCES))
```

### 2. Проверка синтаксиса

```bash
# Проверить Makefile
make -n all
```

### 3. Специальные цели

```makefile
# Всегда устаревшая цель
.PHONY: force
always_rebuild: force
	@echo "Rebuilding..."

# Промежуточные файлы (будут удалены)
.INTERMEDIATE: temp.o

# Драгоценные файлы (не удалять при ошибке)
.PRECIOUS: important.o

# Бесшумная цель
.SILENT: quiet_target
```

### 4. Правила по умолчанию

```makefile
# Отключить встроенные правила
.SUFFIXES:

# Или конкретные суффиксы
.SUFFIXES: .c .o .h
```

### 5. Вторичное расширение

```makefile
# Позволяет использовать переменные в prerequisites
.SECONDEXPANSION:

DEPS_main = utils.o
DEPS_test = utils.o test_utils.o

%: $$(DEPS_$$@)
	$(CC) $^ -o $@
```

---

## Заключение

### Основные принципы хорошего Makefile

1. **Используйте переменные** — гибкость и читаемость
2. **Автоматические зависимости** — используйте `-MD`
3. **Шаблонные правила** — избегайте дублирования
4. **Структура проекта** — разделяйте src, obj, bin
5. **Фиктивные цели** — объявляйте `.PHONY`
6. **Документация** — добавьте цель `help`

### Контрольный список

- [ ] Все команды начинаются с TAB
- [ ] Используются автоматические переменные
- [ ] Есть цели `clean` и `all`
- [ ] Объявлены `.PHONY` цели
- [ ] Зависимости генерируются автоматически
- [ ] Инкрементальная сборка работает
- [ ] Проект структурирован (src/, bin/)
- [ ] Добавлена документация

### Дополнительные ресурсы

- [GNU Make Manual](https://www.gnu.org/software/make/manual/)
- `man make` — справка в терминале
- [Make Tutorial](https://makefiletutorial.com/)

---

**Конец теории** 🎓

Эта теория покрывает все необходимые концепции для выполнения лабораторной работы №3 и создания продвинутых Makefile.
